P14-T209 verifies repository/local database concurrency and transactional-integrity primitives. It covers idempotency uniqueness and row locking, order lifecycle row locking, customer unique-race recovery, SECURITY DEFINER command boundaries, and the absence of direct browser writes. The pgTAP test is rollback-scoped and does not modify production business data. It explicitly does not claim a two-session stress test.