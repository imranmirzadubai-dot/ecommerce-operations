# P10-T153

Direct In Transit to Delivered is implemented via the authoritative `record_delivery_outcome` command. Regression coverage is in `075_direct_in_transit_delivered.sql`. No production data changes.
