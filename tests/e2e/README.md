# T227-001 browser reproduction

This harness is intentionally diagnostic. It records the deployed URL, browser version, navigation timeline, console output, page errors, failed requests, and a screenshot. It does not modify application state or authentication configuration.

The test must be run against a clean-main deployment and the resulting artifact must be retained with the task evidence before T227-001 is closed.
