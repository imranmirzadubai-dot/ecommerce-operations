# E-Commerce Operations — safe configuration template
# Do not place real secrets in this file.

# Local development values only; replace with local Supabase CLI values when the app needs them.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
